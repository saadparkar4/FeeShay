Thanks for the detailed instructions and the UI screenshot. Below is the React Native + Expo code in TypeScript that matches the profile page structure you shared. This version is modular and uses only the packages you've allowed.

📦 No additional libraries were used beyond what you specified.

Directory Structure (Recommended)

mkdir components screens
touch components/ProfileHeader.tsx components/PersonalDetails.tsx components/JobPostCard.tsx components/RatingsAndReviews.tsx components/SettingsPanel.tsx screens/ProfileScreen.tsx



// components/

* ProfileHeader.tsx
* PersonalDetails.tsx
* JobPostCard.tsx
* RatingsAndReviews.tsx
* SettingsPanel.tsx

// screens/

* ProfileScreen.tsx

Let’s start with the main ProfileScreen and break each UI section into reusable components.

1. screens/ProfileScreen.tsx

import React from "react"
import { ScrollView, View } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"
import ProfileHeader from "../components/ProfileHeader"
import PersonalDetails from "../components/PersonalDetails"
import JobPostCard from "../components/JobPostCard"
import RatingsAndReviews from "../components/RatingsAndReviews"
import SettingsPanel from "../components/SettingsPanel"

const ProfileScreen = () => {
return (
\<SafeAreaView style={{ flex: 1 }}>
\<ScrollView contentContainerStyle={{ padding: 16 }}> <ProfileHeader /> <PersonalDetails /> <JobPostCard /> <RatingsAndReviews /> <SettingsPanel /> </ScrollView> </SafeAreaView>
)
}

export default ProfileScreen

2. components/ProfileHeader.tsx

import React from "react"
import { View, Text, Image, Pressable } from "react-native"
import { Ionicons } from "@expo/vector-icons"

const ProfileHeader = () => {
return (
\<View style={{ alignItems: "center", marginBottom: 24 }}>
\<Image
source={{ uri: "[https://via.placeholder.com/100](https://via.placeholder.com/100)" }}
style={{ width: 100, height: 100, borderRadius: 50 }}
/>
\<Pressable
style={{
position: "absolute",
bottom: 0,
right: 120,
backgroundColor: "#fff",
borderRadius: 12,
padding: 4,
}}
\> <Ionicons name="camera" size={16} color="#333" /> </Pressable>
\<Text style={{ fontWeight: "bold", fontSize: 20, marginTop: 12 }}>
Alex Morgan </Text>
\<Text style={{ color: "gray", marginBottom: 6 }}>
Digital Marketing Specialist </Text>
\<Text
style={{
textAlign: "center",
color: "#555",
paddingHorizontal: 16,
}}
\>
Experienced marketing professional with expertise in digital campaigns,
SEO, and content strategy. Looking for talented freelancers to help grow
my business. </Text> </View>
)
}

export default ProfileHeader

3. components/PersonalDetails.tsx

import React from "react"
import { View, Text } from "react-native"
import { Ionicons } from "@expo/vector-icons"

const PersonalDetails = () => {
return (
\<View style={{ marginBottom: 24 }}>
\<Text style={{ fontWeight: "bold", fontSize: 16, marginBottom: 8 }}>
Personal Details </Text>
\<View style={{ flexDirection: "row", alignItems: "center", marginBottom: 4 }}> <Ionicons name="location" size={16} color="purple" />
\<Text style={{ marginLeft: 6 }}>New York, United States</Text> </View>
\<Text style={{ marginTop: 8, marginBottom: 4, fontWeight: "600" }}>
Languages </Text>
\<View style={{ flexDirection: "row", flexWrap: "wrap", gap: 8 }}> <Text style={badgeStyle}>English (Native)</Text> <Text style={badgeStyle}>Spanish (Fluent)</Text> <Text style={badgeStyle}>French (Basic)</Text> </View>
\<View style={{ flexDirection: "row", alignItems: "center", marginTop: 10 }}> <Ionicons name="calendar" size={16} color="purple" />
\<Text style={{ marginLeft: 6 }}>Client since March 2022</Text> </View> </View>
)
}

const badgeStyle = {
backgroundColor: "#f2f2f2",
paddingHorizontal: 8,
paddingVertical: 4,
borderRadius: 8,
fontSize: 12,
marginRight: 6,
}

export default PersonalDetails

4. components/JobPostCard.tsx

import React from "react"
import { View, Text, Pressable } from "react-native"

const jobs = \[
{
title: "Social Media Campaign",
status: "Active",
posted: "3 days ago",
description: "Looking for a social media expert to manage our platforms...",
},
{
title: "Website Content Writer",
status: "Completed",
posted: "2 weeks ago",
description: "Hired a content writer to create engaging articles for our blog...",
},
{
title: "Logo Redesign Project",
status: "In Progress",
posted: "1 week ago",
description: "Working with a designer to refresh our company logo...",
},
]

const JobPostCard = () => {
return (
\<View style={{ marginBottom: 24 }}>
\<View
style={{ flexDirection: "row", justifyContent: "space-between", marginBottom: 12 }}
\>
\<Text style={{ fontWeight: "bold", fontSize: 16 }}>My Job Posts</Text>
\<Text style={{ color: "blue" }}>View All</Text> </View>

```
  {jobs.map((job, index) => (
    <View
      key={index}
      style={{
        backgroundColor: "#f9f9f9",
        padding: 12,
        borderRadius: 12,
        marginBottom: 12,
      }}
    >
      <Text style={{ fontWeight: "bold" }}>{job.title}</Text>
      <Text style={{ color: "gray", marginBottom: 6 }}>{job.description}</Text>
      <View style={{ flexDirection: "row", gap: 8 }}>
        <Text style={statusTag(job.status)}>{job.status}</Text>
        <Text style={{ color: "#888" }}>{job.posted}</Text>
      </View>
    </View>
  ))}
</View>
```

)
}

const statusTag = (status: string) => ({
backgroundColor:
status === "Active"
? "#d1f7c4"
: status === "Completed"
? "#e0e0e0"
: "#fce3aa",
color: "#000",
paddingHorizontal: 8,
borderRadius: 8,
fontSize: 12,
})

export default JobPostCard

5. components/RatingsAndReviews.tsx

import React from "react"
import { View, Text, Image } from "react-native"
import { Ionicons } from "@expo/vector-icons"

const reviews = \[
{
name: "Sarah Johnson",
date: "2 weeks ago",
rating: 5,
comment:
"Great client to work with! Clear instructions and prompt payment. Would definitely work with again.",
},
{
name: "Michael Chen",
date: "1 month ago",
rating: 4,
comment:
"Alex was very professional and provided detailed feedback throughout the project. Communication was excellent.",
},
]

const RatingsAndReviews = () => {
return (
\<View style={{ marginBottom: 24 }}>
\<Text style={{ fontWeight: "bold", fontSize: 16, marginBottom: 8 }}>
Ratings & Reviews </Text>
\<View style={{ flexDirection: "row", alignItems: "center", marginBottom: 8 }}>
\<Text style={{ fontSize: 20, fontWeight: "bold" }}>4.8</Text> <Ionicons name="star" color="gold" size={20} />
\<Text style={{ marginLeft: 6, color: "gray" }}>Based on 12 reviews</Text> </View>
{reviews.map((r, i) => (
\<View key={i} style={{ marginBottom: 12 }}>
\<Text style={{ fontWeight: "600" }}>{r.name}</Text>
\<Text style={{ color: "gray", marginBottom: 4 }}>{r.date}</Text>
\<Text style={{ fontSize: 14, lineHeight: 20 }}>{r.comment}</Text> </View>
))} </View>
)
}

export default RatingsAndReviews

6. components/SettingsPanel.tsx

import React from "react"
import { View, Text, Switch, Pressable } from "react-native"
import { Ionicons } from "@expo/vector-icons"

const SettingsPanel = () => {
return (
\<View style={{ marginBottom: 32 }}>
\<Text style={{ fontWeight: "bold", fontSize: 16, marginBottom: 12 }}>
Settings </Text>

```
  <View style={settingRow}>
    <Ionicons name="notifications" size={20} color="deeppink" />
    <Text style={settingLabel}>Notifications</Text>
    <Switch value={true} />
  </View>

  <View style={settingRow}>
    <Ionicons name="language" size={20} color="purple" />
    <Text style={settingLabel}>Language</Text>
    <Text style={{ marginLeft: "auto" }}>English</Text>
  </View>

  <Pressable style={settingRow}>
    <Ionicons name="log-out-outline" size={20} color="crimson" />
    <Text style={[settingLabel, { color: "crimson" }]}>Logout</Text>
  </Pressable>
</View>
```

)
}

const settingRow = {
flexDirection: "row",
alignItems: "center",
marginBottom: 16,
}

const settingLabel = {
marginLeft: 12,
fontSize: 14,
}

export default SettingsPanel

Would you like this page integrated into your existing navigation (expo-router or react-navigation) or styled with a custom theme/colorset? I can also break out the state and make it dynamic via props or context.
